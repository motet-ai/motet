# Motet runtime dependencies
# Canonical source: pyproject.toml [project.dependencies]
# This file exists for Docker builds and environments that need a flat requirements file.

# Core
pydantic>=2.0.0
pydantic-settings>=2.0.0
typing-extensions>=4.0.0

# Web and API
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=11.0.0
httpx>=0.25.0
aiohttp>=3.9.0
python-multipart>=0.0.6
aiofiles>=23.0.0
jinja2>=3.1.0

# Distributed task execution
celery>=5.3.0
redis>=4.5.0
valkey-glide>=2.5.0
valkey-glide-sync>=2.5.0

# Database and storage
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg[binary]>=3.1.0
boto3>=1.42.97

# LLM providers
# openai >=2.0 required for httpx 0.28+ compatibility (no `proxies` kwarg)
openai>=2.0.0
# anthropic >=0.110 required for adaptive thinking `output_config` (Opus/Sonnet 4.6+)
anthropic>=0.110.0
langchain>=0.1.0
langchain-community>=0.0.10

# Vector databases and embeddings
sentence-transformers>=2.2.0
chromadb>=0.4.0
weaviate-client>=3.25.0
pinecone-client>=2.2.0
bm25-chroma>=0.1.0

# Observability
structlog>=23.0.0
prometheus-client>=0.18.0
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
msgpack>=1.0.0

# CLI and utilities
click>=8.1.0
click-log>=0.4.0
rich>=13.0.0
python-dotenv>=1.0.0

# Security
cryptography>=41.0.0
passlib>=1.7.0
PyJWT>=2.10.1

# Scheduling
schedule>=1.2.0
python-dateutil>=2.8.0
croniter>=1.4.0

# Configuration
pyyaml>=6.0.0
toml>=0.10.0

# Monitoring
flower>=2.0.0
psutil>=5.9.0

# Concurrency backends (ADR-0033)
gevent>=23.9.0
asyncio-gevent
asgiref>=3.7.0

# Host clipboard (local edge tools: core.clipboard_*)
pyclip>=0.7.0

# MCP (Model Context Protocol)
mcp>=1.13,<2

# System management
libtmux>=0.25.0

# Browser automation
playwright>=1.40.0

# Web search (core.web_search non-LLM backend)
ddgs>=9.0.0

# Document extraction and image processing (ADR-0062)
pdfplumber>=0.10
python-docx>=1.1
python-pptx>=1.0.2
odfpy>=1.4.1
striprtf>=0.0.32
openpyxl>=3.1
pypdfium2>=4.0.0
pytesseract>=0.3
Pillow>=11.3.0

# GPU inference (optional — install separately based on platform)
# See pyproject.toml [project.optional-dependencies] gpu
pynvml>=11.5.0
torch>=2.0.0
transformers>=4.30.0
