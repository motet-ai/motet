# Configuration snippets

Committed **YAML and static config** fragments used as references or deployed alongside Motet.

## Files here

Typically include MCP manager profiles (`mcp_instance_manager*.yaml`) and **`model_profiles.yaml`** — exact filenames may evolve; inspect this directory before wiring automation.

Operational secrets belong in Vault or deployment-specific secret stores — **do not** commit real credentials here.
