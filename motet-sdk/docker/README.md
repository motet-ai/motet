## Directory: motet-sdk/docker

**Root-level Compose snippet** for SDK packaging or documentation references (see **`src/motet_sdk/docker/`** for the copy consumed next to CLI code).

Prefer **`motet-cli local`** defaults or **`MOTET_COMPOSE_FILE`** pointing at the Motet repo Compose when developing the full platform.
