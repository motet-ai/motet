format_version: "1"
name: "openai-gateway"
version: "0.1.0"
description: >
  OpenAI-compatible gateway cookbook: Motet as a multi-provider drop-in
  for OpenAI clients (passthrough facade). No agents or tools — operator
  recipe only; see README.
