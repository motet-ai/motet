format_version: "1"
name: "expert-panel"
version: "0.1.0"
description: >
  Multi-agent expert panel that analyzes topics from opposing perspectives
  (optimist vs skeptic), then synthesizes a balanced assessment.  Demonstrates
  agent-segmented memory, cross-agent recall, conversation scoping, and
  workflow-driven parallel execution.
load_order:
  - config
  - tools
  - workflows
